/*
 * Meraki Dashboard API
 * The Cisco Meraki Dashboard API is a modern REST API based on the OpenAPI specification.  > Date: 02 September, 2020 > > [What's New](https://meraki.io/whats-new/)  ---  [API Documentation](https://meraki.io/api)  [Community Support](https://meraki.io/community)  [Meraki Homepage](https://www.meraki.com) 
 *
 * OpenAPI spec version: 1.1.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.15
 *
 * Do not edit the class manually.
 *
 */

(function(root, factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD. Register as an anonymous module.
    define(['ApiClient'], factory);
  } else if (typeof module === 'object' && module.exports) {
    // CommonJS-like environments that support module.exports, like Node.
    module.exports = factory(require('../ApiClient'));
  } else {
    // Browser globals (root is window)
    if (!root.MerakiDashboardApi) {
      root.MerakiDashboardApi = {};
    }
    root.MerakiDashboardApi.UpdateNetworkWirelessSettings = factory(root.MerakiDashboardApi.ApiClient);
  }
}(this, function(ApiClient) {
  'use strict';

  /**
   * The UpdateNetworkWirelessSettings model module.
   * @module model/UpdateNetworkWirelessSettings
   * @version 1.1.0
   */

  /**
   * Constructs a new <code>UpdateNetworkWirelessSettings</code>.
   * @alias module:model/UpdateNetworkWirelessSettings
   * @class
   */
  var exports = function() {
  };

  /**
   * Constructs a <code>UpdateNetworkWirelessSettings</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/UpdateNetworkWirelessSettings} obj Optional instance to populate.
   * @return {module:model/UpdateNetworkWirelessSettings} The populated <code>UpdateNetworkWirelessSettings</code> instance.
   */
  exports.constructFromObject = function(data, obj) {
    if (data) {
      obj = obj || new exports();
      if (data.hasOwnProperty('meshingEnabled'))
        obj.meshingEnabled = ApiClient.convertToType(data['meshingEnabled'], 'Boolean');
      if (data.hasOwnProperty('ipv6BridgeEnabled'))
        obj.ipv6BridgeEnabled = ApiClient.convertToType(data['ipv6BridgeEnabled'], 'Boolean');
      if (data.hasOwnProperty('locationAnalyticsEnabled'))
        obj.locationAnalyticsEnabled = ApiClient.convertToType(data['locationAnalyticsEnabled'], 'Boolean');
      if (data.hasOwnProperty('upgradeStrategy'))
        obj.upgradeStrategy = ApiClient.convertToType(data['upgradeStrategy'], 'String');
      if (data.hasOwnProperty('ledLightsOn'))
        obj.ledLightsOn = ApiClient.convertToType(data['ledLightsOn'], 'Boolean');
    }
    return obj;
  }

  /**
   * Toggle for enabling or disabling meshing in a network
   * @member {Boolean} meshingEnabled
   */
  exports.prototype.meshingEnabled = undefined;

  /**
   * Toggle for enabling or disabling IPv6 bridging in a network (Note: if enabled, SSIDs must also be configured to use bridge mode)
   * @member {Boolean} ipv6BridgeEnabled
   */
  exports.prototype.ipv6BridgeEnabled = undefined;

  /**
   * Toggle for enabling or disabling location analytics for your network
   * @member {Boolean} locationAnalyticsEnabled
   */
  exports.prototype.locationAnalyticsEnabled = undefined;

  /**
   * The upgrade strategy to apply to the network. Must be one of 'minimizeUpgradeTime' or 'minimizeClientDowntime'. Requires firmware version MR 26.8 or higher'
   * @member {module:model/UpdateNetworkWirelessSettings.UpgradeStrategyEnum} upgradeStrategy
   */
  exports.prototype.upgradeStrategy = undefined;

  /**
   * Toggle for enabling or disabling LED lights on all APs in the network (making them run dark)
   * @member {Boolean} ledLightsOn
   */
  exports.prototype.ledLightsOn = undefined;


  /**
   * Allowed values for the <code>upgradeStrategy</code> property.
   * @enum {String}
   * @readonly
   */
  exports.UpgradeStrategyEnum = {
    /**
     * value: "minimizeUpgradeTime"
     * @const
     */
    minimizeUpgradeTime: "minimizeUpgradeTime",

    /**
     * value: "minimizeClientDowntime"
     * @const
     */
    minimizeClientDowntime: "minimizeClientDowntime"
  };

  return exports;

}));
